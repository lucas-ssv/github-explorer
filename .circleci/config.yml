version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.15
    steps:
      - run:
          name: Install packages
          command: npm install
      - run:
          name: The First Step
          command: eslint 'src/**'
      - run:
          name: The Second Step
          command: npm run test:coveralls